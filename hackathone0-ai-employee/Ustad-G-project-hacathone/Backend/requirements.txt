# ── Web Framework ─────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.35

# ── Config & Env ──────────────────────────────────────────────────
python-dotenv>=1.1.0
pydantic-settings>=2.0.0
pydantic[email]>=2.8.0

# ── OpenAI SDK (kept for compatibility) ───────────────────────────────────────
openai>=1.54.0

# ── Google ADK (Agent orchestration — replaces openai-agents) ─────────────────
google-adk>=1.0.0

# ── MCP Server ────────────────────────────────────────────────────
mcp>=1.23.1
fastmcp==2.14.0

# ── HTTP Client ───────────────────────────────────────────────────
httpx>=0.28.1

# ── Google APIs (managed transitively by google-adk) ─────────────
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# ── Utilities ─────────────────────────────────────────────────────
python-dateutil==2.9.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0

# ── Database ──────────────────────────────────────────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# ── Notifications & Scheduler ──────────────────────────────────────────────────
firebase-admin>=6.5.0
apscheduler>=3.10.0

# Task 13: JWT Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
